int main() {
    // Calculates 13th Fibonacci Number (233)
    int lastFib = 1;
    int fib = 1;
    int temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;

    temp = fib + lastFib;
    lastFib = fib;
    fib = temp;
    
    return fib;
}